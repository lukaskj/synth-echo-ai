{
  "name": "@lukaskj/omni-clone",
  "private": true,
  "scripts": {
    "postinstall": "bun run backend:setup",
    "frontend": "bun run --cwd apps/frontend",
    "frontend:dev": "bun run --cwd apps/frontend dev",
    "frontend:start": "bun run --cwd apps/frontend start",
    "backend:setup": "bun scripts/back-setup.ts",
    "backend:dev": "bun scripts/back-run.ts"
  },
  "workspaces": [
    "apps/frontend"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
